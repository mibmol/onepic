import { Header } from "@/components/layout"
import { PagePropsProvider } from "@/lib/hooks"
import { serverSideTranslations } from "next-i18next/serverSideTranslations"
import Head from "next/head"
import { useTranslation } from "next-i18next"
import { SharedHead } from "@/components/layout/header/headUtils"

export const getStaticProps = async ({ locale }) => {
  const localeProps = await serverSideTranslations(locale, ["common"])
  return {
    props: { ...localeProps },
  }
}

const SupportPage = () => {
  const { t } = useTranslation()
  return (
    <PagePropsProvider
      value={{ titleToken: "Support", descriptionToken: "Generated by create next app" }}
    >
      <SharedHead />
      <Header />
      <section className="px-12">Support</section>
    </PagePropsProvider>
  )
}

export default SupportPage
